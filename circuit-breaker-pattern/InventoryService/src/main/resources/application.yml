server:
  port: 9000
spring:
  application:
    name: inventory-service
  datasource:
    url: jdbc:h2:mem:cb-inventory-db
    username: root
    password: 123
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
  h2:
    console:
      enabled: true






